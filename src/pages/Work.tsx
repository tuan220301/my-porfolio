import React from 'react';
import Layout from '../Components/Base/Layout';
import WorkComponent from '../Components/Work';

const WorkPage = ({ path }: any) => {
    return (
        <Layout>
            <WorkComponent />
        </Layout>
    );
};
export default WorkPage;